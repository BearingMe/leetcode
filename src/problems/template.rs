#[allow(warnings)]
#[cfg(test)]
mod ex_test {
    use super::*;

    #[test]
    fn case_1() {
        let input = ();
        let output = ();

        let result = ();

        assert_eq!(output, result);
    }
}
